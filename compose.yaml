services:
  inference:
    build: .
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
      MAX_BATCH_SIZE: "8"
      BATCH_WINDOW_MS: "10"
      MAX_QUEUE_SIZE: "128"
      REQUEST_TIMEOUT_MS: "2000"
      MODEL_LATENCY_MS: "20"
    restart: unless-stopped
